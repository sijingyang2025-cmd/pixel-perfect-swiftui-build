interface:
  display_name: "Image-to-SwiftUI Build"
  short_description: "High-fidelity SwiftUI from image-only UI mockups"
  default_prompt: "Use $pixel-perfect-swiftui-build to implement these PNG/JPG/WebP iOS UI mockups in SwiftUI with measurement, asset extraction, and screenshot overlay QA."

policy:
  allow_implicit_invocation: true
